# Operational State

CURRENT PHASE: Repair 2 in progress; structural-integrity increment validated locally, publication gate pending commit and remote SHA verification.

REMOTE: `origin` configured as `git@github.com:westkitty/Immortals.git`; remote was empty at initialization probe.

CORE LOOP: The active package entry launches a Three.js/WebGL 3D traversal/combat slice with a localStorage envelope, visual scars, compact event ledger, and 100-year return. The older canvas prototypes contain separate experiments and are not integrated into this runtime.

CURRENT CAPABILITIES: Repair 1 adds logical held/pressed/released actions, pointer-lock yaw/pitch, speed-aware chase camera with obstruction pull-in, responsive acceleration and momentum carry, long jump, wall-contact classification, wall run, wall climb, rebound, air dash cooldown, line-of-sight grapple, glide, dive, hard-landing recovery, moving surfable debris, explicit traversal state output, deterministic frame consumption, and a direct browser journey. Repair 2 now adds stateful structure integrity/support failure, collapse transition, visible rubble, bounded combat damage, rival melee pressure, vitality HUD, hit feedback, and browser coverage while preserving the existing history event paths.

LAST VALIDATION: `npm run verify` passes with 14 tests. `npm run test:browser` passes traversal, pause freeze, structure impact, rival attack/response, screenshots, and zero console errors.

NEXT PHASE: Continue Repair 2 — add and verify the remaining combat/destruction behaviors before phase completion.

KNOWN LIMITATIONS: Repair 2 remains incomplete: bridge/support propagation, debris lifecycle, full collapse lifecycle, combat defeat flow, and rigid-body physics remain. The active runtime remains kinematic; later Phase 4–10 code is preserved as experimental scaffolding.
